/**
 * Runtime config for static hosting (GitHub Pages → Render backend).
 *
 * !! BEFORE GOING LIVE:
 *    Replace the URL below with your actual Render service URL.
 *    Format: 'https://<your-render-app-name>.onrender.com'
 *    (no trailing slash, no /api — the code adds /api automatically)
 */
window.LUMITYA_API_BASE = 'https://lumitya-api.onrender.com';
